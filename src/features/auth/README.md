# Auth

NextAuth v4 (JWT). Roles: `admin` | `superadmin` | `driver` | `partner`.

**Import:** `authOptions` from `@/features/auth`. Session provider: `@/features/auth/ui/session-provider`. Logout: `@/features/auth/ui/logout-button`. Models: `@/features/auth/model`.
