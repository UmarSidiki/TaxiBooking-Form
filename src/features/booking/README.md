# Booking

Public 3-step wizard, embeddable forms, cash create, booking patch, maps helpers, customer emails.

**Import:** wizard `@/features/booking/ui/variants/form-v1`, container `@/features/booking/ui/booking-form-container`, services `@/features/booking/lib/*`, models `@/features/booking/model`. Routes stay in `src/app/`.
