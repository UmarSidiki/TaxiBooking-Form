# Settings

Operator Settings document, admin settings tabs, theme, SMTP mailer.

**Import:** model `@/features/settings/model`, theme `@/features/settings/context/theme-context`, `sendEmail` `@/features/settings/lib/email`.
